<!DOCTYPE html>
<html lang="en">
    {{>header}}

    <body>
        {{>navbar}}
        <div class="uk-container">
            <h2 class="uk-margin-top">{{title}}</h2>
            <div class="uk-card uk-card-primary uk-width-1-3@m">
                <div class="uk-card-header">
                    <div class="uk-grid-small uk-flex-left" uk-grid>
                        <div class="uk-width-expand">
                            <h3 class="uk-card-title uk-margin-remove-bottom">{{capitalize user.username}}</h3>
                        </div>
                    </div>
                </div>
                <div class="uk-card-body">
                    <p>Email: {{user.email}}</p>
                    <p>Gender: {{user.gender}}</p>
                    <p>Age: {{user.age}}</p>
                </div>
                <div class="uk-card-footer">
                    <a href="#" class="uk-button uk-button-text">Contact</a>
                </div>
        </div>
        </div>
    </body>

</html>